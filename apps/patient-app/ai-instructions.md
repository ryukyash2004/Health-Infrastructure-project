Tech Stack: Next.js (App Router), TypeScript, Tailwind CSS, Lucide React icons.

Backend URL: http://localhost:8000/api/v1 (FastAPI).

Design Rules: Mobile-first, strict Apple-like minimalism, never use generic placeholder colors, always use proper React state.