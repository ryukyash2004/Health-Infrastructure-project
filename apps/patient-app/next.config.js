/** @type {import('next').NextConfig} */
const nextConfig = {
  // Move it to the top level here
  allowedDevOrigins: ['192.168.29.207'],
  
  // You can completely remove the experimental block if it is now empty
};

export default nextConfig;